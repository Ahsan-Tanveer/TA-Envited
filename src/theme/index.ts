import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: '#000000',
    },
    secondary: {
      main: '#888888',
    },
  },
  typography: {
    h1: {
      fontSize: '3.5rem',
    },
  },
});